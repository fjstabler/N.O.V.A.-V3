"""Built-in skills.

Every module in this package is scanned at boot and any :class:`~nova.skills.base.Skill`
subclass it defines is registered. Dropping a new module here is all it takes to
add a capability — nothing else in the codebase needs to change.
"""
